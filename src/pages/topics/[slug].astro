---
const { slug } = Astro.params;
const slugMap: Record<string, string> = {
  "stock-return-concentration": "return-concentration",
};
const target = slugMap[slug ?? ""] ? `/research/${slugMap[slug!]}/` : "/research/";
return Astro.redirect(target, 301);
---
